<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/APYROPROJECT.styles.css" asp-append-version="true" />




    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"></script>

    <style>

        body 
        {
            margin: 0;
            padding: 0;
            width: 100vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        canvas {
            position: absolute;
            display: none;
        }

        #videoInput {
            opacity: 0;
        }
    </style>
</head>
<body>
    <div>
       @*  <video id="videoInput" width="720" height="550" muted controls></video> *@





        <button type="button" class="btn btn-success" data-toggle="modal" data-target="#myModal">
            Enable Student Tracker
        </button>

        <!-- Modal -->
        <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="Video" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content" style="background-color:#1A222B; color:white">
                    <div class="modal-header">
                        <h5 class="modal-title" id="exampleModalLongTitle">Video</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <video id="videoInput" width="720" height="550" muted controls></video>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-success" data-dismiss="modal" onclick="stopVideo()">Done</button>
                    </div>
                </div>
            </div>
        </div>






@* 
        <label for="input">Choose a video file to process:</label>
        <input type="file" id="input" name="input_video" accept="video/mp4, video/mov">
        <video id="video" width="720" height="550" muted controls ></video> *@


@*         FOR MODAL VIDEO RECOGNITION *@
      @*   <div class="text-white">
            <label for="input">Choose a video file to process:</label>
            <input type="file"
                   id="input"
                   name="input_video"
                   accept="video/mp4, video/mov" />
         
        </div> *@


@*         <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#myModal">
            Launch demo modal
        </button> *@

        @*         FOR MODAL VIDEO RECOGNITION *@
        <!-- Modal -->
       @*  <div class="modal fade"
             id="myModal"
             tabindex="-1"
             role="dialog"
             aria-labelledby="Video"
             aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content" style="background-color:#1A222B; color:white">
                    <div class="modal-header">
                        <h5 class="modal-title" id="exampleModalLongTitle">Video</h5>
                        <button type="button"
                                class="close"
                                data-dismiss="modal"
                                onclick="stopVideo()"
                                aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <video 
                               id="video"
                               controls
                               style=" " width="720" height="550" muted></video>
                        <div id="canvasContainer1" style="position: absolute; top: 46%; left: 50%; transform: translate(-50%, -50%);"></div>
                    </div>
                    <div class="modal-footer">
                        <button type="button"
                                class="btn btn-success"
                                data-dismiss="modal"
                                onclick="stopVideo()">

                            Done
                        </button>

                    </div>
                </div>
            </div>
        </div>
 *@








        <script defer src="~/face-api.min.js"></script>
        <script defer src="~/videoattendance.js"></script>
        @* 

        <label for="input">Choose a video file to process:</label>
        <input type="file" id="input" name="input_video" accept="video/mp4, video/mov">
        <video id="video" width="320" height="240" controls style="display: none;"></video> *@



@* 
        <script>
            document.getElementById("input").addEventListener("change", function () {
                var media = URL.createObjectURL(this.files[0]);
                var video = document.getElementById("video");
                video.src = media;
                video.style.display = "block";
                video.play();
            });

        </script> *@



        @RenderBody()
    </div>
</body>
</html>
